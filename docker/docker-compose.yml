services:
  backend:
    build:
      context: ../backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./data/kiyodesk.db
    volumes:
      - kiyodesk_data:/app/data

volumes:
  kiyodesk_data:
